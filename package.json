{
  "name": "whispr",
  "version": "1.0.0",
  "description": "Whispr - Application de messagerie anonyme",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Build static files' && mkdir -p www && cp index.html www/",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
